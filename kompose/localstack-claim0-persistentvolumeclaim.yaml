apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: localstack-claim0
  name: localstack-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
