apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: localstack-claim2
  name: localstack-claim2
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
